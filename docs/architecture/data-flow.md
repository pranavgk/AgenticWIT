# AgenticWIT Data Flow Diagrams

## Authentication Flow

### User Registration Flow

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. POST /api/auth/register
     │    { email, username, password, firstName, lastName }
     ↓
┌──────────────────────────────────────────────────────┐
│              Fastify API Server                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 1. Rate Limit Check (5 req/15min)              │ │
│  │    ├─ Pass → Continue                          │ │
│  │    └─ Fail → 429 Too Many Requests             │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 2. Input Validation (Zod schema)               │ │
│  │    ├─ Email format                             │ │
│  │    ├─ Username length (3-30 chars)             │ │
│  │    ├─ Password strength (8+ chars, mixed)      │ │
│  │    ├─ Required fields present                  │ │
│  │    └─ Fail → 400 Validation Error              │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 3. Check Uniqueness                            │ │
│  │    Query PostgreSQL:                           │ │
│  │    - Email exists? → 400 Already Exists        │ │
│  │    - Username exists? → 400 Already Exists     │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 4. Hash Password                               │ │
│  │    bcrypt.hash(password, 12)                   │ │
│  │    ⏱ ~200-300ms (intentionally slow)           │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 5. Create User                                 │ │
│  │    INSERT INTO User                            │ │
│  │    - id (generated UUID)                       │ │
│  │    - email, username                           │ │
│  │    - password (hashed)                         │ │
│  │    - firstName, lastName                       │ │
│  │    - default accessibility preferences         │ │
│  │    - timestamps                                │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 6. Generate Tokens                             │ │
│  │    - Access Token (JWT, 15 min)                │ │
│  │      Payload: { userId, email }                │ │
│  │      Sign with JWT_SECRET                      │ │
│  │    - Refresh Token (random 64-byte hex, 7d)    │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 7. Store Refresh Token                         │ │
│  │    INSERT INTO RefreshToken                    │ │
│  │    - token                                     │ │
│  │    - userId (FK to User)                       │ │
│  │    - expiresAt (now + 7 days)                  │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 8. Log Audit Event                             │ │
│  │    INSERT INTO AuditLog                        │ │
│  │    - action: 'register'                        │ │
│  │    - userId                                    │ │
│  │    - ipAddress, userAgent                      │ │
│  │    - timestamp                                 │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 9. Sanitize Response                           │ │
│  │    Remove sensitive fields:                    │ │
│  │    - password (hashed)                         │ │
│  │    - mfaSecret                                 │ │
│  └─────────────────┬──────────────────────────────┘ │
└────────────────────┼──────────────────────────────┘
                     │ 10. 201 Created
                     │     { user, accessToken, refreshToken }
                     ↓
              ┌─────────┐
              │ Client  │
              │ Stores tokens in:
              │ - Memory (access token)
              │ - HttpOnly cookie (refresh token)
              └─────────┘
```

### User Login Flow

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. POST /api/auth/login
     │    { email, password }
     ↓
┌──────────────────────────────────────────────────────┐
│              Fastify API Server                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 1. Rate Limit Check (10 req/15min)             │ │
│  │    Prevents brute force attacks                │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 2. Input Validation                            │ │
│  │    - Email format                              │ │
│  │    - Password present                          │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 3. Find User                                   │ │
│  │    SELECT * FROM User WHERE email = ?          │ │
│  │    Not found → 401 Invalid Credentials         │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 4. Check Account Status                        │ │
│  │    - suspended → 403 Account Suspended         │ │
│  │    - deleted → 401 Invalid Credentials         │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 5. Verify Password                             │ │
│  │    bcrypt.compare(password, user.password)     │ │
│  │    ⏱ ~200-300ms (timing-safe)                  │ │
│  │    No match → 401 Invalid Credentials          │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 6. Generate New Tokens                         │ │
│  │    - New access token (15 min)                 │ │
│  │    - New refresh token (7 days)                │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 7. Clean Up Old Refresh Tokens                 │ │
│  │    DELETE FROM RefreshToken                    │ │
│  │    WHERE userId = ? AND expiresAt < NOW()      │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 8. Store New Refresh Token                     │ │
│  │    INSERT INTO RefreshToken                    │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 9. Update Last Login                           │ │
│  │    UPDATE User SET                             │ │
│  │    lastLoginAt = NOW(),                        │ │
│  │    lastLoginIp = ?                             │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 10. Log Audit Event                            │ │
│  │    action: 'login'                             │ │
│  └─────────────────┬──────────────────────────────┘ │
└────────────────────┼──────────────────────────────┘
                     │ 11. 200 OK
                     │     { user, accessToken, refreshToken }
                     ↓
              ┌─────────┐
              │ Client  │
              └─────────┘
```

### Protected API Request Flow

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. GET /api/users/me
     │    Authorization: Bearer <access-token>
     ↓
┌──────────────────────────────────────────────────────┐
│              Fastify API Server                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 1. CORS Check                                  │ │
│  │    Verify origin is allowed                    │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 2. Security Headers                            │ │
│  │    Add Helmet headers                          │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 3. Rate Limit Check                            │ │
│  │    Check general API rate limit               │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 4. Authentication Middleware                   │ │
│  │    ┌────────────────────────────────────────┐  │ │
│  │    │ a. Extract Token from Header           │  │ │
│  │    │    Authorization: "Bearer <token>"     │  │ │
│  │    │    Missing → 401 Unauthorized          │  │ │
│  │    └────────────┬───────────────────────────┘  │ │
│  │                 ↓                                │ │
│  │    ┌────────────────────────────────────────┐  │ │
│  │    │ b. Verify JWT Signature                │  │ │
│  │    │    jwt.verify(token, JWT_SECRET)       │  │ │
│  │    │    Invalid → 401 Invalid Token         │  │ │
│  │    └────────────┬───────────────────────────┘  │ │
│  │                 ↓                                │ │
│  │    ┌────────────────────────────────────────┐  │ │
│  │    │ c. Check Token Expiration              │  │ │
│  │    │    Expired → 401 Token Expired         │  │ │
│  │    └────────────┬───────────────────────────┘  │ │
│  │                 ↓                                │ │
│  │    ┌────────────────────────────────────────┐  │ │
│  │    │ d. Decode Payload                      │  │ │
│  │    │    Extract userId from JWT             │  │ │
│  │    └────────────┬───────────────────────────┘  │ │
│  │                 ↓                                │ │
│  │    ┌────────────────────────────────────────┐  │ │
│  │    │ e. Fetch User from Database            │  │ │
│  │    │    SELECT * FROM User WHERE id = ?     │  │ │
│  │    │    Not found → 401 User Not Found      │  │ │
│  │    │    Deleted → 401 Account Deleted       │  │ │
│  │    │    Suspended → 403 Account Suspended   │  │ │
│  │    └────────────┬───────────────────────────┘  │ │
│  │                 ↓                                │ │
│  │    ┌────────────────────────────────────────┐  │ │
│  │    │ f. Attach User to Request              │  │ │
│  │    │    request.currentUser = user          │  │ │
│  │    └────────────┬───────────────────────────┘  │ │
│  └─────────────────┼──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 5. Route Handler                               │ │
│  │    Access user via request.currentUser         │ │
│  │    Execute business logic                      │ │
│  │    Query database as needed                    │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 6. Sanitize Response                           │ │
│  │    Remove sensitive fields                     │ │
│  └─────────────────┬──────────────────────────────┘ │
└────────────────────┼──────────────────────────────┘
                     │ 7. 200 OK
                     │    { success: true, data: {...} }
                     ↓
              ┌─────────┐
              │ Client  │
              └─────────┘
```

### Token Refresh Flow

```
┌─────────┐
│ Client  │
│ (Detects access token expired)
└────┬────┘
     │ 1. POST /api/auth/refresh
     │    { refreshToken }
     ↓
┌──────────────────────────────────────────────────────┐
│              Fastify API Server                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 1. Validate Input                              │ │
│  │    refreshToken present and non-empty          │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 2. Find Refresh Token                          │ │
│  │    SELECT * FROM RefreshToken                  │ │
│  │    WHERE token = ?                             │ │
│  │    Not found → 401 Invalid Token               │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 3. Check Expiration                            │ │
│  │    IF expiresAt < NOW()                        │ │
│  │    → DELETE token                              │ │
│  │    → 401 Token Expired                         │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 4. Fetch User                                  │ │
│  │    SELECT * FROM User WHERE id = userId        │ │
│  │    Check account status                        │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 5. Generate New Tokens                         │ │
│  │    - New access token (15 min)                 │ │
│  │    - New refresh token (7 days)                │ │
│  │    🔄 Token Rotation for security              │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 6. Delete Old Refresh Token                    │ │
│  │    DELETE FROM RefreshToken WHERE token = ?    │ │
│  │    (Prevents reuse attacks)                    │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 7. Store New Refresh Token                     │ │
│  │    INSERT INTO RefreshToken                    │ │
│  └─────────────────┬──────────────────────────────┘ │
└────────────────────┼──────────────────────────────┘
                     │ 8. 200 OK
                     │    { accessToken, refreshToken }
                     ↓
              ┌─────────┐
              │ Client  │
              │ Updates stored tokens
              │ Retries original request
              └─────────┘
```

### Logout Flow

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. POST /api/auth/logout
     │    Authorization: Bearer <access-token>
     │    { refreshToken }
     ↓
┌──────────────────────────────────────────────────────┐
│              Fastify API Server                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 1. Authenticate User                           │ │
│  │    Verify JWT access token                     │ │
│  │    Attach user to request                      │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 2. Delete Refresh Token                        │ │
│  │    DELETE FROM RefreshToken                    │ │
│  │    WHERE token = ? AND userId = ?              │ │
│  │    (Prevents future token refresh)             │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 3. Add Access Token to Blacklist (Optional)    │ │
│  │    Redis: SET "blacklist:<token>" 1 EX 900     │ │
│  │    TTL = remaining token lifetime              │ │
│  │    (Immediate invalidation of access token)    │ │
│  └─────────────────┬──────────────────────────────┘ │
│                    ↓                                 │
│  ┌────────────────────────────────────────────────┐ │
│  │ 4. Log Audit Event                             │ │
│  │    action: 'logout'                            │ │
│  │    userId, ipAddress, timestamp                │ │
│  └─────────────────┬──────────────────────────────┘ │
└────────────────────┼──────────────────────────────┘
                     │ 5. 200 OK
                     │    { success: true, message: "Logout successful" }
                     ↓
              ┌─────────┐
              │ Client  │
              │ Clears stored tokens
              │ Redirects to login
              └─────────┘
```

## Profile Management Flow

### Get Profile

```
Client → GET /api/users/me (with Bearer token)
       → Auth Middleware (verify JWT)
       → Fetch user from PostgreSQL
       → Sanitize response (remove password, mfaSecret)
       → 200 OK { user data }
```

### Update Profile

```
Client → PATCH /api/users/me (with Bearer token)
       → Auth Middleware
       → Validate input (Zod schema)
       → Check email/username uniqueness (if changed)
       → UPDATE User SET ... WHERE id = currentUser.id
       → Return updated user data
```

### Change Password

```
Client → POST /api/users/me/password (with Bearer token)
       → Auth Middleware
       → Rate Limit Check (5 req/15min)
       → Validate input (Zod schema)
       → Verify current password (bcrypt.compare)
       → Validate new password strength
       → Hash new password (bcrypt, 12 rounds)
       → UPDATE User SET password = ? WHERE id = ?
       → Invalidate all refresh tokens (force re-login on all devices)
       → Log audit event
       → 200 OK
```

## Error Handling Flow

```
Request → Middleware/Handler
        ↓
   [Error Occurs]
        ↓
   Error Handler (setErrorHandler)
        ├─ Log error (with stack trace in dev)
        ├─ Determine status code
        │  ├─ Validation Error → 400
        │  ├─ Unauthorized → 401
        │  ├─ Forbidden → 403
        │  ├─ Not Found → 404
        │  ├─ Rate Limit → 429
        │  └─ Server Error → 500
        ├─ Sanitize error message (no sensitive data)
        └─ Return formatted response
              { success: false, message: "...", error: {...} }
```

## Database Transaction Flow

### Complex Operation Example (Change Email)

```
1. BEGIN TRANSACTION
2. SELECT * FROM User WHERE email = new_email FOR UPDATE
3. IF email exists → ROLLBACK → 400 Email Already Exists
4. UPDATE User SET email = new_email WHERE id = user_id
5. INSERT INTO AuditLog (action = 'email_change', ...)
6. COMMIT TRANSACTION
7. Return success

If any step fails:
→ ROLLBACK TRANSACTION
→ Return appropriate error
```

## Rate Limiting Flow

```
Incoming Request
    ↓
Rate Limit Middleware
    ├─ Generate Key: IP address + Endpoint
    ├─ Check Redis: GET "ratelimit:<key>"
    ├─ Current Count < Max?
    │   ├─ Yes: Increment count
    │   │       Redis: INCR "ratelimit:<key>"
    │   │       Set TTL if first request
    │   │       Allow request → Continue
    │   └─ No: Block request
    │           Return 429 Too Many Requests
    │           Headers:
    │           - X-RateLimit-Limit
    │           - X-RateLimit-Remaining
    │           - X-RateLimit-Reset
    └─
```

## Caching Strategy Flow

### Read-Through Cache Pattern

```
Request Data
    ↓
1. Check Redis Cache
    ├─ Cache HIT
    │   └─ Return cached data (fast path)
    │
    └─ Cache MISS
        ↓
    2. Query PostgreSQL
        ↓
    3. Store in Redis (with TTL)
        ↓
    4. Return data

Example: User profile data
- TTL: 5 minutes
- Invalidated on profile update
```

### Write-Through Cache Pattern

```
Update Data
    ↓
1. Write to PostgreSQL
    ↓
2. Update Redis Cache
   OR
   Delete from Redis (lazy regeneration)
    ↓
3. Return success

Example: User preferences update
- Update DB first (source of truth)
- Invalidate cache
- Next read will repopulate
```

---

**Last Updated**: November 24, 2025
**Version**: 0.1.0
